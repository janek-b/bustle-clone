{{#if admin.isAdmin}}
  {{#bs-button type="primary navbar-btn" onClick=(action "removeAdmin")}}Admin Log out{{/bs-button}}
{{else}}
  {{#bs-button type="primary navbar-btn" onClick=(action "makeAdmin")}}Admin Log in{{/bs-button}}
{{/if}}
