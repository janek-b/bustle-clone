{{#bs-button active=notCollapsed onClick=(action "toggle")}}
  {{#if collapsed}}
    Add Story
  {{else}}
    Hide
  {{/if}}
{{/bs-button}}

{{#bs-collapse collapsed=collapsed}}
  <br>
  <div class="well">
    <form>
      <div class="form-group">
        <label for="title">Title</label>
        {{input value=title id="title" class="form-control"}}
      </div>
      <div class="form-group">
        <label for="author">Author</label>
        {{input value=author id="author" class="form-control"}}
      </div>
      <div class="form-group">
        <label for="img">Image</label>
        {{input value=img id="img" class="form-control"}}
      </div>
      <div class="form-group">
        <label for="text">Content</label>
        {{textarea value=text id="text" rows="6" class="form-control"}}
      </div>
      {{#bs-button type="success" onClick=(action "saveStory")}}Save{{/bs-button}}
    </form>
  </div>
{{/bs-collapse}}
<hr>
